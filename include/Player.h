#ifdef PLAYER_H
#define PLAYER_H
#include <MultipartComponent.h>
/*
class Player : public MultipartComponent
{
public:
    static SmartPointer<Player> createInstance(int x, int y, int w, int h, bool has_collision, int hp) {
        return SmartPointer<Player>(new Player(x, y, w, h, has_collision));
    }
    void tick() override;
    void move(Direction direction);
    void shoot();
    void damage(int amount);
    int get_hp() const {
        return hp;
    }
protected:
*/

#endif